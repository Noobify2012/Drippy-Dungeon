package controller;

/**
 * Enum representing the types of actions a player can make including no action. This is used to
 * help keep track of the state and control input to the model.
 */
public enum ActionEnum {
  NONE,
  MOVE,
  SHOOT,
  PICKUP
}
